# 06. 実装計画と未決事項

## 1. 実装タスクリスト（確認用）

実装着手前に、本リストと未決事項の回答を確定する。

| # | タスク | 成果物 | 状態 |
|---|--------|--------|------|
| T-01 | 設計仕様のレビュー・未決事項確定 | 本ドキュメント更新 | 待ち |
| T-01b | 資料照合（07）の P0 取り込み方針確定 | 07レビュー＋02仕様 | **実施済（要承認）** |
| T-02 | 入力フォームへ病薬オプションスイッチ追加 | `sidepanel.html` / CSS / FormRenderer | 未着手 |
| T-03 | AppController に feature flag と計算分岐を実装 | `AppController.js` | 未着手 |
| T-03b | `KishouAssessor`（寒暖燥湿清濁）新規 | `js/core/KishouAssessor.js` + テスト | 未着手 |
| T-03c | 用神損傷（継善）解析＋月令ブースト從重 | `ByoyakuCalculator` 拡張 | 未着手 |
| T-03d | 調候方向と十神薬の突合・病薬バランス | `ByoyakuCalculator` + 表示 | 未着手 |
| T-04 | ResultRenderer を病薬セクション独立表示に整理（気象→主軸→病薬） | `ResultRenderer.js` | 未着手 |
| T-05 | 現行結果内 `#kakkyoku-toggle` を廃止／置換 | HTML + JS | 未着手 |
| T-06 | （任意）オプション永続化 | `chrome.storage` + manifest permissions | 未着手 |
| T-07 | 既存テスト実行＋三篇シナリオテスト | `npm test` / 手動手順 | 未着手 |
| T-08 | USER_GUIDE / manifest 説明の更新 | docs / manifest | 未着手 |

## 2. 推奨実装順序

```text
T-01（仕様確定）
  → T-02（スイッチUI）
  → T-03（計算分岐）※本体)
  → T-04 / T-05（表示）
  → T-06（永続化・任意）
  → T-07（検証）
  → T-08（ドキュメント）
```

## 3. 未決事項（実装前に確認）

### D-01. 大運吉凶（DaiunHyouka）の扱い

| 選択肢 | 内容 |
|--------|------|
| A | 病薬OFF時は実行しない。病薬ON時も本変更では非表示のまま（既存トグル依存） |
| B | 病薬ON時は従来どおり評価まで実行し、既存トグルで詳細表示 |
| C | 大運吉凶も別オプション化する（スコープ拡大・非推奨） |

**設計側の推奨: B**（既存挙動を壊しにくい）  
ただし病薬OFF時は必ずスキップする。

### D-02. 合冲セクションの表示

| 選択肢 | 内容 |
|--------|------|
| A | 病薬パイプライン内でのみ計算し、**合冲UIは出さない**（内部利用のみ） |
| B | 病薬ON時は現行どおり合冲セクションも出す |
| C | 合冲も独立オプション（スコープ拡大） |

**設計側の推奨: A**（本変更の主題を病薬に絞る）

### D-03. 格局・身旺弱のユーザー表示

| 選択肢 | 内容 |
|--------|------|
| A | 病薬セクション内に補助情報として表示（推奨） |
| B | 完全非表示（内部計算のみ） |
| C | 別セクションで常時表示 |

**設計側の推奨: A**

### D-04. スイッチ変更時の再計算

| 選択肢 | 内容 |
|--------|------|
| A | 自動再計算 |
| B | 「再計算してください」と案内のみ |
| C | 表示だけ切り替え（計算は常時両方）← **不採用** |

**設計側の推奨: A**

### D-05. オプション永続化

| 選択肢 | 内容 |
|--------|------|
| A | セッション内のみ |
| B | `chrome.storage.local` で永続化 |

**設計側の推奨: 初回は A、必要なら B を追加**

### D-06. デフォルト値

| 選択肢 | 内容 |
|--------|------|
| A | OFF（命式のみ）← 本設計の方針 |
| B | ON（現行に近い） |

**設計側の推奨: A**

### D-07. 気象と格局別病薬が矛盾した時の優先

| 選択肢 | 内容 |
|--------|------|
| A | 気象（調候）を常に最優先の病／薬とする |
| B | 継善の用神損傷を最優先し、気象は副病として併記（推奨） |
| C | 格局ルールのみ（気象は表示だけ）← 不採用 |

**設計側の推奨: B**（07レビュー 4.4 統合ルール）

### D-08. 気象の清濁を初版で実装するか

| 選択肢 | 内容 |
|--------|------|
| A | 寒暖・燥湿のみ初版。清濁は P2 |
| B | 清濁も簡易ルールで初版から入れる |

**設計側の推奨: A**

## 4. リスク

| リスク | 影響 | 緩和 |
|--------|------|------|
| 病薬が格局依存のため、病薬ONで格局計算が必須 | 計算時間増 | ON時のみ実行 |
| UIから格局表現を消すと理由が分かりにくい | 理解低下 | 補助表示（D-03-A） |
| 既存ユーザーが常時病薬表示に慣れている | 戸惑い | ガイド更新・スイッチを目立たせる |
| 大運吉凶と病薬の依存 | OFF時に吉凶が空になる | D-01 で明示 |

## 5. 非目標（再掲）

- 病薬アルゴリズムの全面再設計（理論精緻化は別タスク）
- 禄命法対応
- 蓋頭・動静・運勢論の新規実装
- 格局／合冲の独立オプション化（将来課題）

## 6. 次アクション

1. 本仕様書一式をレビューする  
2. **D-01〜D-06** を回答する  
3. 回答反映後、T-02 から実装に入る  
