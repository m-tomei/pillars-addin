# 06. 実装計画と決定事項

設計レビューは **承認**（PR#11）。D-01〜D-08および R-01〜R-03 は完了済。現在 Sprint B3。

| ID | 実装前のP0確認 | 完了条件 |
|----|----------------|----------|
| R-01 | 四病四薬の規則、とくに「長」の根拠不足 | **完了**（v1.1: 四薬4値、雕の処置分離、長の成立/反例） |
| R-02 | 月令×2.0、從重40%、気象閾値の校正 | **完了（暫定確定）**（閾値fixture表＋JSON。実装後T-07で再校正可） |
| R-03 | UI受入用の生年月日時が未固定 | **完了**（受入シナリオ表 S-01〜S-07＋acceptance_scenarios.json） |

## 1. 実装タスクリスト（確認用）

実装着手前に、本リストと未決事項の回答を確定する。

| # | タスク | 成果物 | 状態 |
|---|--------|--------|------|
| T-01 | 設計仕様のレビュー・未決事項確定 | 本ドキュメント更新 | **完了**（PR#10、条件付き承認） |
| T-01b | 資料照合（07）の P0 取り込み方針確定 | 07レビュー＋02仕様 | **完了** |
| T-01c | R-01〜R-03 の根拠・fixture確定 | 資料追補＋テストデータ表 | **完了**（PR#11） |
| T-02 | 入力フォームへ病薬オプションスイッチ追加 | `sidepanel.html` / CSS / FormRenderer | **完了** |
| T-03 | AppController に feature flag と計算分岐を実装 | `AppController.js` / `byoyakuPipeline.js` | **完了**（D-04計算済みガード含む） |
| T-03a | `KeizenAnalyzer`（主軸・用神損傷） | `js/core/KeizenAnalyzer.js` + テスト | **完了**（KZ-01〜04相当） |
| T-03b | `KishouAssessor`（寒暖・燥湿、清濁=`不明`）※格局より前 | `js/core/KishouAssessor.js` + テスト | **完了**（標準柱ウェイト適用） |
| T-03c | 月令ブースト從重＋四病四薬接続 | `ByoyakuCalculator` 拡張 | **完了** |
| T-03d | 調候方向と十神薬の突合・病薬バランス | `ByoyakuCalculator` + 単体テスト | **完了**（Keizen breaks接続含む） |
| T-03e | `diagnose(object)` v2 API 化 | `ByoyakuCalculator` | **完了** |
| T-04 | ResultRenderer（気象→主軸→病薬→バランス→喜忌） | `ResultRenderer.js` | **完了** |
| T-05 | 現行結果内 `#kakkyoku-toggle` を廃止／置換 | HTML + JS | **完了** |
| T-06 | オプション永続化 | — | **見送り**（D-05） |
| T-07 | 既存テスト＋三篇シナリオ＋OFF時非実行spy | `npm test` / 手動手順 | **完了**（S-01〜S-07自動受入含む） |
| T-08 | USER_GUIDE / manifest 説明の更新 | docs / manifest | **完了**（v2.0.0） |

詳細なフェーズ分割は [11_実行計画.md](../病薬表示機能_詳細設計/11_実行計画.md) を正とする。

## 2. 推奨実装順序

```text
T-01 / T-01b / T-01c（完了・PR#11）
  → Sprint B1: T-02 / T-03 / T-05 / OFF時spy  ← 完了
  → Sprint B2: T-03b / a / e / c / d 完了
  → Sprint B3: T-04 / T-07 / T-08 完了  ← PR待ち
```

## 3. 決定事項

### D-01. 大運吉凶（DaiunHyouka）の扱い

| 選択肢 | 内容 |
|--------|------|
| A | 病薬OFF時は実行しない。病薬ON時も本変更では非表示のまま（既存トグル依存） |
| B | 病薬ON時は従来どおり評価まで実行し、既存トグルで詳細表示 |
| C | 大運吉凶も別オプション化する（スコープ拡大・非推奨） |

**設計側の推奨: B**（既存挙動を壊しにくい）
ただし病薬OFF時は必ずスキップする。

**決定: B。**

### D-02. 合冲セクションの表示

| 選択肢 | 内容 |
|--------|------|
| A | 病薬パイプライン内でのみ計算し、**合冲UIは出さない**（内部利用のみ） |
| B | 病薬ON時は現行どおり合冲セクションも出す |
| C | 合冲も独立オプション（スコープ拡大） |

**設計側の推奨: A**（本変更の主題を病薬に絞る）

**決定: A。合冲は内部入力としてのみ利用し、本変更では合冲UIを表示しない。**

### D-03. 格局・身旺弱のユーザー表示

| 選択肢 | 内容 |
|--------|------|
| A | 病薬セクション内に補助情報として表示（推奨） |
| B | 完全非表示（内部計算のみ） |
| C | 別セクションで常時表示 |

**設計側の推奨: A**

**決定: A。**

### D-04. スイッチ変更時の再計算

| 選択肢 | 内容 |
|--------|------|
| A | 自動再計算 |
| B | 「再計算してください」と案内のみ |
| C | 表示だけ切り替え（計算は常時両方）← **不採用** |

**設計側の推奨: A**

**決定: A。入力が有効で計算済みの場合のみ自動再計算し、未入力・無効入力ではフラグ更新だけを行う。**

### D-05. オプション永続化

| 選択肢 | 内容 |
|--------|------|
| A | セッション内のみ |
| B | `chrome.storage.local` で永続化 |

**設計側の推奨: 初回は A、必要なら B を追加**

**決定: A。初版ではセッション内のみ。`storage` 権限は追加しない。**

### D-06. デフォルト値

| 選択肢 | 内容 |
|--------|------|
| A | OFF（命式のみ）← 本設計の方針 |
| B | ON（現行に近い） |

**設計側の推奨: A**

**決定: A。**

### D-07. 気象と格局別病薬が矛盾した時の優先

| 選択肢 | 内容 |
|--------|------|
| A | 気象（調候）を常に最優先の病／薬とする |
| B | 継善の用神損傷を最優先し、気象は副病として併記（推奨） |
| C | 格局ルールのみ（気象は表示だけ）← 不採用 |

**設計側の推奨: B**（07レビュー 4.4 統合ルール）

**決定: B。用神損傷を主病、気象を副病とする。主病がなく気象偏りだけが重大な場合に限り、気象診断を先頭表示できる。**

### D-08. 気象の清濁を初版で実装するか

| 選択肢 | 内容 |
|--------|------|
| A | 寒暖・燥湿のみ初版。清濁は P2 |
| B | 清濁も簡易ルールで初版から入れる |

**設計側の推奨: A**

**決定: A。初版は `clarity: '不明'` 固定とし、清／濁を推測表示しない。**

## 4. リスク

| リスク | 影響 | 緩和 |
|--------|------|------|
| 病薬が格局依存のため、病薬ONで格局計算が必須 | 計算時間増 | ON時のみ実行 |
| UIから格局表現を消すと理由が分かりにくい | 理解低下 | 補助表示（D-03-A） |
| 既存ユーザーが常時病薬表示に慣れている | 戸惑い | ガイド更新・スイッチを目立たせる |
| 大運吉凶と病薬の依存 | OFF時に吉凶が空になる | D-01 で明示 |

## 5. 非目標（再掲）

- 病薬アルゴリズムの全面再設計（理論精緻化は別タスク）
- 禄命法対応
- 蓋頭・動静・運勢論の新規実装
- 格局／合冲の独立オプション化（将来課題）

## 6. 次アクション

1. ~~Phase A / Sprint B1 / Sprint B2 コア~~ → **完了**
2. **Sprint B3:** T-04 / T-07 / T-08 **完了**
3. T-07 受入 S-01〜S-07 / T-08 ガイド・manifest

進捗の単一参照先: [11_実行計画.md](../病薬表示機能_詳細設計/11_実行計画.md)
