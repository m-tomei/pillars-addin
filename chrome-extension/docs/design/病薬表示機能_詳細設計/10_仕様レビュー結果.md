# 10. 仕様レビュー結果

| 項目 | 内容 |
|------|------|
| 対象 | 病薬表示オプション／病薬表示機能 詳細設計 |
| 参照資料 | `資料/病薬説` `喜忌篇` `継善篇` `気象篇` ほか |
| レビュー日 | 2026-08-09 |
| 初回判定 | 条件付き承認（PR#10） |
| 判定（再更新） | **条件付き承認**（ゲートレビュー修正PRのマージで承認） |
| 対象範囲 | 仕様書のみ（実装変更なし） |

## 1. 結論

命式本体と病薬オプションの分離、`日干→月令→気象→主軸→損傷→病薬→喜忌` の順序、用神損傷を主病・気象を副病とする方針は参照資料と整合する。

初回レビュー時の実装前3点に対する成果物を再監査した結果、R-01〜R-03の接続に実装を阻害する不整合が見つかった。本レビュー差分で解消し、マージを承認条件とする。

1. ~~四病四薬「長」の根拠不足~~ → R-01 v1.1（生/長分離、雕の処置を四薬から分離）
2. ~~閾値の校正不足~~ → R-02 v1.1（数値と正負境界を機械可読fixtureへ固定）
3. ~~UI受入入力未固定~~ → R-03 v1.1（S-01〜S-07、長の成立例と反例を分離）

本修正PRの内容を取り込めば実装開始条件を満たす。詳細は [06_実装計画と未決事項](../病薬表示オプション/06_実装計画と未決事項.md)。

## 2. 修正した仕様上の問題

| 重要度 | 問題 | 修正 |
|--------|------|------|
| 高 | 上位仕様は気象を格局・用神より先とする一方、詳細フローは格局の後に気象を実行していた | `KishouAssessor` を日干・月令確定直後へ移動 |
| 高 | `diagnose` が位置引数維持とobject引数必須の二重定義だった | v2正規APIをobject引数に統一。位置引数は移行アダプタ限定 |
| 高 | 気象病の `disease.element` に不足側＝薬五行を入れる定義になっていた | 原因不明時は `null`、`causeElements` と `deficientElements` を分離 |
| 中 | 上位仕様では清濁が必須、詳細設計では初版見送りだった | 初版は `clarity: '不明'` 固定に統一 |
| 中 | アーキテクチャ文書が `ByoyakuCalculator` 原則変更なしとしていた | 月令ブースト・從重接続・気象／継善突合・バランス追加を明記 |
| 中 | 気象extremeを四病の独自値のように扱う余地があった | `fourDisease` は四値を維持し、気象は別診断として管理 |
| 中 | 気象診断を表示先頭にすると、既存大運吉凶がnullの病五行を参照しうる | 表示順と代表診断を分離し、null時の片側評価／スキップを契約化 |
| 中 | 合冲UIの扱いが未決だった | D-02=A。病薬の内部入力に限り、UIは表示しない |
| 中 | OFF時の受入が「非表示」しか確認せず、非実行を保証しなかった | optional calculatorが呼ばれないspy結合テストを追加 |
| 高 | `琢` を四薬の第5 enumに追加し、分類名とAPI契約が食い違っていた | `fourMedicine` は4値+nullに戻し、雕の処置を `treatmentMode` へ分離 |
| 高 | 四病・四薬分類の五行と、代表となる具体診断の五行を同じフィールドへ載せる余地があった | `fourDiseaseElement` / `fourMedicineElement` を代表 `disease` / `medicine` から分離 |
| 高 | S-06は月令加重後に土が約42%であり、木の `長` 成立例になっていなかった | S-06を損の反例に変更し、木が約47%のS-07を追加 |
| 中 | TH-1は値だけ、気象境界は正側だけで機械的に検証できなかった | 月支主気0.7×2=1.4と寒暖・燥湿の正負3点境界をJSON化 |
| 中 | 土の長養三方が「簡易は月支季節」と未定義だった | 土は初版の長判定対象外とし、旺の損へフォールバック |

## 3. 資料との整合確認

### 整合している点

- [喜忌篇](../../../../資料/喜忌篇_初心者から中級者向け解説.md): 日干を主、月令を提綱、透干を先に見る順序
- [気象篇](../../../../資料/気象篇_初心者から中級者向け解説.md): 用神・病薬の前に寒暖燥湿など局全体を確認する方針
- [継善篇](../../../../資料/継善篇_初心者から中級者向け解説.md): 用神を傷つける要素を病候補とする方針
- [病薬説](../../../../資料/病薬説_初心者から中級者向け解説.md): 月令起点の從重、病と薬の所在・強弱・バランス

### 定量根拠に限界がある点

- 原文が長養三方を具体的に示すのは木と火。金・水は対称規則として採用し、土は初版対象外とした
- 数値ウェイト・閾値は資料中の定量値ではなく、実装可能にするための仮説である
- 清濁は資料上重要だが、件数だけの簡易判定では意味を狭めるため初版から外す

## 4. 確定した設計判断

| ID | 決定 |
|----|------|
| D-01 | 病薬OFFでは大運吉凶を実行しない。ONでは既存トグル用評価を維持 |
| D-02 | 合冲は内部利用のみ。合冲UIは出さない |
| D-03 | 格局・身旺弱は病薬セクションの補助情報として表示 |
| D-04 | 計算済みかつ入力有効ならスイッチ変更時に自動再計算 |
| D-05 | 初版はセッション内保持のみ |
| D-06 | デフォルトOFF |
| D-07 | 用神損傷を主病、気象を副病 |
| D-08 | 清濁は初版見送り、`不明` 固定 |

## 5. 実装開始ゲート

次をすべて満たしてから実装タスクへ進む。

- [x] R-01: 四病四薬の根拠・判定表・代表例を資料追補し、v1.1でゲート不整合を修正
  - 追補: `資料/病薬説_…` 第20章
  - 判定表: [四病四薬_判定表.md](./四病四薬_判定表.md)
  - 要点: 生＝長生滋養、長＝帝旺克制。雕は四薬null、処置ラベル `treatmentMode=琢`
- [x] R-02: 数値閾値の境界fixtureと暫定確定（**2026-08-09**）
  - [閾値fixture表.md](./閾値fixture表.md)／[fixtures/threshold_fixtures.json](./fixtures/threshold_fixtures.json)
- [x] R-03: UI受入用の入力値・期待四柱・期待病薬を固定（**2026-08-09**）
  - [受入シナリオ表.md](./受入シナリオ表.md)／[fixtures/acceptance_scenarios.json](./fixtures/acceptance_scenarios.json)
- [x] 更新後の [レビューチェックリスト](./09_レビューチェックリスト.md) で△項目を再判定
  - D3（四病優先・生/長）を含む。総合判定は本修正PRマージまで **条件付き承認**
