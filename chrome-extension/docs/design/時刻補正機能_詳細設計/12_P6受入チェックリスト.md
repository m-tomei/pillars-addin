# P6 結果表示・PNG 受入チェックリスト

対象フェーズ: **P6（結果表示・PNG）**  
実施日: 2026-08-13  
ブランチ: `feat/v1.5-p6-result-summary`

自動テストは `ResultRenderer.correction.test.js` / `ResultRenderer.capture.test.js` で確認する。実機PNGの目視は P8 で最終確認する。

## UI-04 結果に「時刻補正: 適用」と内訳

- [x] 見出し「時刻補正」
- [x] 「時刻補正: 適用」
- [x] 入力時刻 / 時差 / 地方平均時補正 / 補正後時刻 / 子時モード
- [x] 東京都は `+19分（東京都 / 東経139.6917°）`
- [x] 時差 0 でも `+00:00（0分）` を出す（補正量0でも内訳あり）

確認方法: DOMアサーション（P6-T1 / UI-04）

## UI-05 時刻なしで1行サマリ

- [x] `時刻補正: 時刻未入力のため補正・時柱なし` のみ
- [x] `<ul>` / `<h3>` を出さない

確認方法: DOMアサーション（P6-T1 / UI-05）

## UI-07 PNGにサマリ

- [x] `#time-correction-summary` が `#result-section` 配下、かつ `#fortune-result` より前（既存 `ImageExporter.exportToPNG(#result-section)` のキャプチャ対象）
- [x] `.correction-summary` の CSS がある
- [ ] 実機で PNG を保存し、サマリ文字列が画像に写る（**P8 目視**）

確認方法: HTML/CSS配置の自動テスト + P8 目視

## UI-09 表示年が補正後年（年跨ぎ時）

- [x] `showResults` の `displayYear` を大運カード年に使う（例: 2023-12-31 23:50 +20分 → 2024年）

確認方法: DOMアサーション（P6-T4 / UI-09）。配線自体は P5 で `displayYear = t_corrected.year`。

## その他（P6ゲート）

- [x] `switch23` かつ補正後 hour===23 で「注: 23時切替のため日柱は翌日扱い（YYYY-MM-DD）」
- [x] 年末 23時切替は翌日が翌年（2024-01-01）
- [x] `switch00` の 23時台には注記なし
- [x] `clear()` でサマリ・命式・大運を空にし、サマリと結果セクションを非表示
